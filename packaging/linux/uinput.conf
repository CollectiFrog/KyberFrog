# KyberFrog — make sure the uinput module is present at boot.
#
# Remote control injects mouse buttons and keystrokes through /dev/uinput. The
# node only exists once the module is loaded, and nothing else on a plain
# desktop necessarily loads it, so a first remote-control attempt after a fresh
# boot would fail on a missing device rather than on a permission.
uinput
