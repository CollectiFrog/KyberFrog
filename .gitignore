/target
/dist
**/*.rs.bk

# tauri-build derived output (JSON schemas regenerated on every build)
/kyberfrog/gen/

# Cargo.lock is intentionally committed: kyberfrog is a deployable application
# and we want reproducible builds.
